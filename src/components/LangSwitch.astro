---
import { altPath, t, type Lang } from '../i18n';

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;
const dict = t(lang);
const target = dict.langSwitch.target;
const href = altPath(Astro.url.pathname, target);
---

<a
  href={href}
  hreflang={target === 'en' ? 'en' : 'uk'}
  aria-label={dict.langSwitch.full}
  title={dict.langSwitch.full}
  class="inline-flex h-9 min-w-[2.75rem] items-center justify-center rounded-full border border-line bg-card px-3 text-sm leading-none font-semibold text-muted transition-colors hover:border-accent hover:text-accent"
  >{dict.langSwitch.short}</a
>
